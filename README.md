This repository contains a complex systems simulation modeling financial contagion and liquidity cascades within Venture Capital networks using a Barabási–Albert scale-free topology. 
To run the simulation, install the required dependencies (`pip install networkx matplotlib scipy numpy`) and execute the python script.
